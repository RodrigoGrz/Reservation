generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model PropertyTypes {
  id String @id @default(uuid())
  type String
  created_at DateTime

  @@map("property_types")
}